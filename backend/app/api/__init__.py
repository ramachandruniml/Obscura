"""HTTP layer.

Deliverable 5:
    routes_redact.py - POST /redact  (file + method + threshold -> job id)
    routes_jobs.py   - GET /jobs/{id} (status + result URL), GET /jobs/{id}/result
    deps.py          - upload validation (type, size, duration, pixel bomb)
"""
